{
  "name": "task-tracker",
  "module": "app.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun app.ts",
    "dev": "bun --watch app.ts",
    "testing": "bun test --watch --coverage"
  }
}